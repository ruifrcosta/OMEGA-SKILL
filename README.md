# OMEGA TITAN X OMEGA — RELENTLESS ENGINEERING OPERATING SYSTEM

## UNCOMPROMISING BIG TECH SOFTWARE FACTORY
## UNIFIED SKILL FORGE PROTOCOL ∞

Welcome to the official repository of **OMEGA**, an autonomous, Big Tech-grade Engineering Operating System, self-evolving AI-native software factory, and persistent organizational intelligence engine.

OMEGA replaces all fragmented, isolated, or duplicated AI developer workflows, establishing a single unified truth engine and executive multi-agent runtime.

---

# THE OMEGA COMMANDMENT: ZERO COMPROMISE

OMEGA never compromises on quality, safety, or architecture. If a developer or a subagent attempts to skip a phase, bypass documentation, or introduce low-effort solutions (such as raw secrets in code, unindexed queries, default "linear" animations, or standard boring card layouts), OMEGA **HALTS THE PIPELINE** and enforces strict compliance gates.

Before writing a single line of code, OMEGA automatically runs its internal **Self-Interrogation and Audit Gates**:

```
                       ┌──────────────────────────┐
                       │     OMEGA AUDIT GATES    │
                       └──────────────────────────┘
                                    │
       ┌────────────────────────────┼────────────────────────────┐
       ▼                            ▼                            ▼
[Architecture Gate]           [Security Gate]             [Aesthetic Gate]
- Scalable to 10x-100x?       - Zero raw secrets in code?  - "No Cards" enforced?
- Monotonic PKs in MySQL?     - HttpOnly auth cookies?     - Spacing on 4/8dp grid?
- No random UUIDs as PKs?     - Strict CSP configured?     - Tactile press active?
- Composite indexes order?    - Zero Trust mTLS active?    - Concentric curves calc?
- Cursor pagination limit?    - CORS origins hardened?     - Dynamic theme morphing?
```

---

# CORE DOMAIN TAXONOMY (OBSIDIAN MEMORY VAULT)

Every architectural blueprint, decision (ADRs), workflow standard, and runbook lives permanently inside the 33-Folder Obsidian organizational memory vault (`obsidian-vault/`).

### Obsidian Domain Routing Index

1. **`00-Executive/`**: OKRs, high-level business goals, and strategic quarterly plans.
2. **`01-Architecture/`**: System blueprints, hexagonal models, and DDD bounded context maps.
3. **`02-Product/`**: Product specifications, user stories, and detailed roadmaps.
4. **`03-Infrastructure/`**: Terraform modules, VPC configurations, and `[[database-blueprints]]`.
5. **`04-Frontend/`**: Next.js App Router settings, global state, and `[[motion-choreography]]`.
6. **`05-Backend/`**: API contracts, database designs, and background task queue models.
7. **`06-Mobile/`**: Expo builds, OTA update records, and local secure storage.
8. **`07-AI/`**: Prompt architectures, model sanitizers, and LangGraph states.
9. **`08-Data/`**: Kafka event streams, Airflow DAG models, and ETL pipeline maps.
10. **`09-Security/`**: Threat modeling, CORS/CSP safety settings, and `[[security-guardrails]]`.
11. **`10-Observability/`**: SLO metrics, Grafana links, and Prometheus alert rules.
12. **`11-Design-System/`**: Spacing tokens, UI palettes, and `[[ultra-minimalism]]`.
13. **`12-RBAC/`**: Roles permission tables and granular IAM policies.
14. **`13-Sprints/`**: Weekly agile sprints and active developer checklists.
15. **`14-ADRs/`**: Central Architectural Decision Records:
    - `ADR-0002`: Autonomous AI Agent standards (inherit models, explicit triggers).
    - `ADR-0003`: High-End Aesthetics standards (Nested Bezel, banned fonts).
    - `ADR-0004`: Google Workspace standards (Model Armor, Zsh history escapes).
    - `ADR-0005`: DB & Deploy standards (Monotonic clustered PKs, Vercel token env injection).
    - `ADR-0006`: Secure Client and Pentesting standards (Electron isolates, Shannon).
    - `ADR-0007`: FinOps and Self-Healing standards (RTK logs, prompt caches, automated SOPs).
    - `ADR-0008`: Ultra-Minimalist Category dynamic styling mapping standards.
16. **`15-Runbooks/`**: Troubleshooting procedures, deploy steps, and disaster steps.
17. **`16-Incidents/`**: Post-mortems, crash histories, and SRE remediation records.
18. **`17-Diagrams/`**: Raw Mermaid codes and BPMN XML configurations.
19. **`18-Knowledge-Graph/`**: Tag indices, ontology mappings, and wiki-links index.
20. **`19-Standards/`**: `[[google-workspace]]` command formats, formatting lints, and CLI presets.
21. **`20-QA/`**: Playwright Axe E2E tests and Vitest coverage reports.
22. **`21-Compliance/`**: ISO 27001, ISO 9001 checklists, and SOC 2 reports.
23. **`22-Token-Optimization/`**: Rust Token Killer (RTK) prefixes and prompt caching benchmarks.
24. **`23-Cost-Optimization/`**: Database pooling settings, and serverless cold-start warmers.
25. **`24-Agent-Orchestration/`**: Central subagent hierarchy registries.
26. **`25-Platform-Engineering/`**: `[[deployment-blueprints]]` for Vercel CLI token deployments and workspaces.
27. **`26-Workflow-Systems/`**: `[[gws-recipes]]` for Gmail/Sheets/Meet automation.
28. **`27-Developer-Experience/`**: Local bootstrap instructions and development specifications.
29. **`28-Disaster-Recovery/`**: Failover procedures, backup schedules, and recovery trials.
30. **`29-Audit-Logs/`**: Admin activities logs and compliance changelogs.
31. **`30-Kubernetes/`**: Helm charts specs, pod topologies, and active manifests.
32. **`31-MCP/`**: Model Context Protocol integrations and server tools.
33. **`32-AI-Memory/`**: Agent milestone history and error memory.
34. **`33-Architecture-Recovery/`**: System drift check tools and pre-push `omega-cli.py audit` configurations.

---

# UNCOMPROMISING DOMAIN MANDATES

### 1. The Dynamic Visual-Silence Mandate
OMEGA completely bans generic "AI slop" styles (banned: Inter, Roboto, Arial, Lucide icons, sticky top navs, drop-shadows, and cards).
- **Whitespace over Enclosures**: Visual organization comes strictly from whitespace rhythm (4/8dp grid), weight hierarchy, and near-invisible separators (`border-black/5`). No visual card containers are allowed.
- **Concentric curve alignment**: All nested bezel containers ("Doppelrand") must mathematically calculate nested inner radii to match outer parameters: `rounded-[calc(OUTER-PADDING)]`.
- **Dynamic Brand Morphing**: Colors and typography are **never** static. OMEGA morphs the brand theme dynamically based on the project's target category:
  - *Healthcare*: Emerald green (`#059669`) + clinical, readable Figtree/DM Sans.
  - *Finance*: Structured Royal Blue (`#1D4ED8`) + institutional Söhne/IBM Plex.
  - *Productivity*: violet (`#7C3AED`) + functional, clean Geist Sans.
  - *E-Commerce*: Pitch Black (`#0F0F0F`) + high-contrast serif Playfair Display.

### 2. High-Performance Motion Mandate
Standard `linear` or `ease-in-out` transitions are completely banned.
- Enforce spring physics with high-damping cubic-beziers: `transition-all duration-700 ease-[cubic-bezier(0.32,0.72,0,1)]`.
- Entrance reveals must animate from `scale(0.95)` and `opacity: 0` to create tactile physical depth.
- GSAP ScrollTriggers must cleanly register, use scrub lag ratios for catch-up lag, use `ease: "none"` for horizontal scrolling, and completely dispose of timelines on unmount.

### 3. Google Workspace (GWS) Mandate
- **Safe Authentication**: Enforce `GOOGLE_APPLICATION_CREDENTIALS` service JSON injection or `gws auth login`. Raw API keys are strictly banned.
- **Model Armor Sanitization**: Any public-facing write/send operations (such as drafting emails or creating documents) that process raw user inputs **MUST** wrap parameters with the Model Armor sanitization flag: `--sanitize <TEMPLATE>`.
- **Zsh History Escape**: Sheet ranges inside commands must be double-quoted to prevent Zsh shell history expansion failures (e.g. `gws sheets +read --range "Sheet1!A1:D10"`).

### 4. Database & Deployment Mandate
- **Monotonic Clustered PKs**: High-write transactional MySQL tables must utilize auto-increment integers (`BIGINT UNSIGNED AUTO_INCREMENT`). Random UUID values are strictly banned as clustered primary keys.
- **Composite indexing**: Enforce composite indexing placing equality columns first, followed by range/sorting columns (leftmost prefix rule).
- **Secure Token Injection**: The `VERCEL_TOKEN` must strictly be injected as an environment variable in the running context. Passing `--token` directly in CLI arguments is banned.
- **Staging Preview Gates**: Deployments must default to `preview` environments. Pushes to `production` are banned unless explicitly approved by the PMO and SRE agents.

### 5. Application Security & Pentest Mandate
- **Boundary Validation**: Every system edge (HTTP endpoints, database queries, file uploads, IPC messages) must validate and sanitize data prior to internal ingestion.
- **Secure Cookies & Hashing**: Auth cookies must carry `HttpOnly`, `Secure`, and `SameSite=Strict/Lax` flags. Passwords must strictly be hashed via Argon2id.
- **Electron Sandboxing**: Enforce `nodeIntegration: false`, `contextIsolation: true`, `sandbox: true`, and custom validated `contextBridge` interfaces.
- **Exploit Validation (Shannon)**: Vulnerability reports must only log exploits verified via a containerized reproducible Proof-of-Concept (PoC).

### 6. FinOps & Self-Healing Mandate
- **Rust Token Killer (RTK)**: All diagnostic, test, and build commands must carry the `rtk` prefix to compress shell console logs and prevent AI context-window token bloat.
- **Prompt Caching**: Place highly static instructions and system templates at the absolute beginning of context inputs to trigger gateway caching checkpoints.
- **Drift Sweeps**: Verify OMEGA repositories prior to pushes using the native CLI compliance audit:
  ```bash
  python scripts/omega-cli.py audit
  ```

---

## The OMEGA CLI

OMEGA includes a built-in Python command line utility (`scripts/omega-cli.py`) to manage local developer compliance, generate ADR templates, and audit security leaks.

### Initializing the Vault
To bootstrap or repair the 33-Folder structure in a new directory:
```bash
python scripts/omega-cli.py init-vault --dir /path/to/project/
```

### Formulate Architectural Decision Records
To generate a formatted, standardized ADR file under OMEGA's governance guidelines:
```bash
python scripts/omega-cli.py create-adr --title "your-decision" --status accepted --deciders "CTO Agent"
```

### Continuous Compliance Audit
To scan local repository files for hardcoded keys, permissive CORS policies, or structure violations prior to pushes:
```bash
python scripts/omega-cli.py audit
```
